<Window x:Class="GrandadAudioPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GrandadAudioPlayer"
        xmlns:xaml="clr-namespace:GrandadAudioPlayer.xaml"
        mc:Ignorable="d"
        Title="MainWindow" Height="726.282" Width="1212.179" WindowState="Maximized"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">
    <Grid Width="auto" Height="auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="0,0,1,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Label Content="Choose what to play:" FontSize="36"></Label>
            <local:FolderView Grid.Row="1"></local:FolderView>
        </Grid>
        <xaml:PlaylistView Grid.Column="1"></xaml:PlaylistView>

    </Grid>


</Window>
